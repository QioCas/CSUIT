Không có đồng nhất số sau dấu chấm thập phân.
Chỉ ghi ra con số thôi.

Nếu số trước là 1 chữ số 0 thì lấy 4 chữ, còn lại lấy 2 chữ số.

Viết dấu chấm thập phân chứ 0 phải dấu phải thập phân.

Acc, Prcíion Recall chỉ cần ghi phân số luôn.

KNN là thuật toán máy học dùng cho bài toán học có dám sát.
Đặc điểm tính distance x tới các điểm dữ liệu.

Tùy thuộc vào độ đo khoảng cách.

f(x, y) = 2x+3y+5
f'(x, y)=(2,3)
là or.
Đạo hàm khảo sát độ biến thiên, độ thay đổi, sự biến đổi theo biến đang xét.

Ta không thể biết được sự thay đổi trên 2 biến.

f' = (delta f / delta x, delta f / delta y)
`f' = (f'_x, f_y)`
`f'_x`, `f_y` là đạo ham vi phần.

Ta tính đạo hàm vi phần chứ chưa tính đạo hàm cùng lúc.
vector -> score -> prob -> labels